let name=prompt("Enter the name:","Roopasri");
let a=prompt("Enter the start slice");
let b=prompt("Enter the end slice");
if((a!=NaN) || (b!=NaN))
{
  alert("Enter a number");
  let a=prompt("Enter the start slice");
  let b=prompt("Enter the end slice");
  alert(name.slice(parseInt(a),parseInt(b)));
}
  alert(name.slice(parseInt(a),parseInt(b)));


